name: Build OTA Package

on:
  # push:
  #   branches: main
  workflow_dispatch:
    inputs:
      use_upx:
        description: 'build with upx'
        required: false
        type: boolean
        default: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-ota:
    name: Build OTA Package
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Read VERSION
        id: version
        run: |
          VERSION=$(cat VERSION | tr -d '[:space:]')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Version: $VERSION"

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-musl

      - name: Install cross-compilation toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools
          musl-gcc --version

      - name: Install UPX
        if: inputs.use_upx == true
        run: |
          sudo apt-get install -y upx-ucl
          upx --version

      - name: Validate version numbers
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          BACKEND_VERSION=$(sed -n 's/^version = "\([^"]*\)"/\1/p' backend/Cargo.toml | head -n 1)
          FRONTEND_VERSION=$(sed -n 's/.*"version": "\([^"]*\)".*/\1/p' frontend/package.json | head -n 1)

          echo "VERSION=$VERSION"
          echo "backend/Cargo.toml=$BACKEND_VERSION"
          echo "frontend/package.json=$FRONTEND_VERSION"

          if [ "$BACKEND_VERSION" != "$VERSION" ]; then
            echo "backend/Cargo.toml version does not match VERSION"
            exit 1
          fi

          if [ "$FRONTEND_VERSION" != "$VERSION" ]; then
            echo "frontend/package.json version does not match VERSION"
            exit 1
          fi

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: backend -> target
          cache-targets: true
          cache-all-crates: true

      - name: Build backend (aarch64-unknown-linux-musl)
        working-directory: backend
        run: |
          echo "🦀 构建后端 (aarch64-unknown-linux-musl)..."

          export CC_AARCH64_UNKNOWN_LINUX_MUSL=musl-gcc
          export CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER=musl-gcc
          export SQLITE3_STATIC=1
          export LIBSQLITE3_SYS_USE_PKG_CONFIG=0

          cargo build --locked --release --target aarch64-unknown-linux-musl

          BINARY_PATH="target/aarch64-unknown-linux-musl/release/udx710"

          echo "后端构建完成:"
          ls -lh "$BINARY_PATH"
          file "$BINARY_PATH"

      - name: Compress binary with UPX
        if: inputs.use_upx == true
        run: |
          BINARY_PATH="backend/target/aarch64-unknown-linux-musl/release/udx710"

          echo "使用 UPX 压缩..."
          BEFORE_SIZE=$(stat -c%s "$BINARY_PATH")

          upx --best --lzma "$BINARY_PATH"

          AFTER_SIZE=$(stat -c%s "$BINARY_PATH")
          RATIO=$(echo "scale=1; 100 - ($AFTER_SIZE * 100 / $BEFORE_SIZE)" | bc)

          echo "压缩完成，节省 ${RATIO}%"
          ls -lh "$BINARY_PATH"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Build frontend
        working-directory: frontend
        run: |
          echo "🎨 构建前端..."
          pnpm install --frozen-lockfile --prefer-offline
          pnpm run build

          echo "前端构建完成:"
          ls -lh dist/

      - name: Package OTA
        id: package
        env:
          # Ed25519 私钥（base64 编码的 32 字节原始种子）。在仓库 Secrets 中配置。
          # 缺失则打包会直接失败（固件会拒绝无签名的 OTA 包）。
          OTA_SIGN_KEY_B64: ${{ secrets.OTA_SIGN_KEY_B64 }}
        run: |
          echo "=========================================="
          echo "  生成 OTA 更新包"
          echo "=========================================="
          echo ""

          VERSION="${{ steps.version.outputs.version }}"
          COMMIT=$(git rev-parse --short HEAD)
          BUILD_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          ARCH="aarch64-unknown-linux-musl"

          BINARY_PATH="backend/target/aarch64-unknown-linux-musl/release/udx710"
          FRONTEND_DIR="frontend/dist"

          if [ ! -f "$BINARY_PATH" ]; then
            echo "错误: 后端二进制不存在"
            exit 1
          fi

          if [ ! -d "$FRONTEND_DIR" ]; then
            echo "错误: 前端构建产物不存在"
            exit 1
          fi

          OTA_TMP=$(mktemp -d)

          echo "版本: $VERSION"
          echo "Commit: $COMMIT"
          echo "构建时间: $BUILD_TIME"
          echo "架构: $ARCH"
          echo ""

          echo "复制后端二进制..."
          cp "$BINARY_PATH" "$OTA_TMP/udx710"
          chmod 755 "$OTA_TMP/udx710"

          BINARY_MD5=$(md5sum "$OTA_TMP/udx710" | cut -d' ' -f1)
          echo "binary md5: $BINARY_MD5"

          echo "复制前端文件..."
          mkdir -p "$OTA_TMP/www"
          cp -r "$FRONTEND_DIR"/* "$OTA_TMP/www/"

          echo "计算前端 MD5..."
          FRONTEND_MD5=$(find "$OTA_TMP/www" -type f -exec md5sum {} \; | cut -d' ' -f1 | sort | md5sum | cut -d' ' -f1)
          echo "frontend md5: $FRONTEND_MD5"

          cat > "$OTA_TMP/meta.json" <<EOF
          {
              "version": "$VERSION",
              "commit": "$COMMIT",
              "build_time": "$BUILD_TIME",
              "binary_md5": "$BINARY_MD5",
              "frontend_md5": "$FRONTEND_MD5",
              "arch": "$ARCH"
          }
          EOF

          cat "$OTA_TMP/meta.json"
          echo ""

          # ==================== Ed25519 签名 OTA 包 ====================
          # 固件侧（ota.rs）强制校验 meta.json 的 signature 字段，未签名/验签失败一律拒绝安装。
          echo "🔏 对 meta.json 进行 Ed25519 签名..."
          if [ -z "${OTA_SIGN_KEY_B64}" ]; then
            echo "❌ 错误: 未配置仓库 Secret OTA_SIGN_KEY_B64，无法签名 OTA 包（固件会拒绝）"
            exit 1
          fi
          echo "${OTA_SIGN_KEY_B64}" | base64 -d > "$OTA_TMP/ota_sign_key.bin"
          python3 -m pip install --break-system-packages --quiet cryptography 2>/dev/null \
            || pip install --break-system-packages --quiet cryptography 2>/dev/null \
            || pip install --quiet cryptography 2>/dev/null \
            || true
          python3 scripts/sign_ota.py \
            --meta "$OTA_TMP/meta.json" \
            --binary "$OTA_TMP/udx710" \
            --key "$OTA_TMP/ota_sign_key.bin"
          echo "✅ 签名完成，meta.json 已写入 signature 字段"
          cat "$OTA_TMP/meta.json"
          echo ""

          # 创建输出目录
          mkdir -p release

          OTA_FILE="release/udx710-ota-${VERSION}.tar.gz"
          echo "打包 OTA 更新包..."
          cd "$OTA_TMP"
          tar -czf "$GITHUB_WORKSPACE/$OTA_FILE" meta.json udx710 www
          cd "$GITHUB_WORKSPACE"

          echo ""
          echo "=========================================="
          echo "OTA 更新包打包完成"
          echo "=========================================="
          echo ""
          echo "输出文件: $OTA_FILE"
          ls -lh "$OTA_FILE"
          echo ""
          echo "包内容:"
          tar -tzf "$OTA_FILE" | head -20
          echo ""

          OTA_MD5=$(md5sum "$OTA_FILE" | cut -d' ' -f1)
          echo "OTA md5: $OTA_MD5"
          echo ""

          echo "ota_file=$OTA_FILE" >> "$GITHUB_OUTPUT"
          echo "ota_md5=$OTA_MD5" >> "$GITHUB_OUTPUT"

      - name: Upload OTA package
        uses: actions/upload-artifact@v6
        with:
          name: udx710-ota-${{ steps.version.outputs.version }}
          path: release/udx710-ota-${{ steps.version.outputs.version }}.tar.gz
          retention-days: 90
          if-no-files-found: error
      # ==================== 更新 latest Release ====================
      - name: Update latest Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: "Build (${{ steps.version.outputs.version }})"
          files: release/udx710-ota-${{ steps.version.outputs.version }}.tar.gz
          body: |
            ## OTA 更新包

            **版本**: ${{ steps.version.outputs.version }}
            **Commit**: ${{ github.sha }}
            **构建时间**: ${{ github.event.repository.updated_at }}
            **触发方式**: ${{ github.event_name }}

            ### Web 页面一键升级

            1. 打开 http://192.168.66.1:3000 → OTA 更新
            2. 下载下方 `udx710-ota-${{ steps.version.outputs.version }}.tar.gz`
            3. 上传到设备的 OTA 页面 → 应用并重启

            ### 命令行升级

            ```bash
            # 下载
            wget https://github.com/${{ github.repository }}/releases/download/latest/udx710-ota-${{ steps.version.outputs.version }}.tar.gz

            # 上传
            curl -X POST http://192.168.66.1:3000/api/ota/upload \
              -F "file=@udx710-ota-${{ steps.version.outputs.version }}.tar.gz"

            # 应用
            curl -X POST http://192.168.66.1:3000/api/ota/apply \
              -H "Content-Type: application/json" \
              -d '{"restart_now": true}'
            ```
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
          append_body: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Summary
        run: |
          {
            echo "## OTA 包构建完成"
            echo ""
            echo "| 项目 | 值 |"
            echo "|------|-----|"
            echo "| 版本 | ${{ steps.version.outputs.version }} |"
            echo "| Commit | ${{ github.sha }} |"
            echo "| 架构 | aarch64-unknown-linux-musl |"
            echo "| UPX 压缩 | ${{ inputs.use_upx }} |"
            echo ""
            echo "### 产物"
            echo "- \`${{ steps.package.outputs.ota_file }}\`"
            echo "- MD5: \`${{ steps.package.outputs.ota_md5 }}\`"
          } >> "$GITHUB_STEP_SUMMARY"
